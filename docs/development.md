# Development

*Content coming in a later PR (migration from README).*

Will cover:

- Editable install (`pip install --no-deps -e .`).
- Running the test suite (`pytest tests/`).
- Repo layout / code map (`src/hexenium/{cli,pipeline,config,layout}.py`,
  `src/hexenium/stages/*`).
- Contributing conventions.

In the meantime, the [README's Development &
testing section](https://github.com/settylab/heRegistration/blob/v0.2.0/README.md#development--testing)
covers the test suite basics.
